# CONTEXT.md — Memoria viva del proyecto SECOP Insight

> Este archivo es la memoria del agente y del equipo. Aquí se registra cómo pensamos, por qué decidimos y en qué estado real está el proyecto. Léelo antes de cualquier sesión de explicación.

## 1. Quiénes somos y cómo trabajamos
- **Equipo:** Grupo 8 ADSO 3171062 — Steven Alejandro Araque Castro (dev principal, principiante guiado) y Yesid Amaya — Instructor Gustavo Jiménez Suancha (CIMM).
- **Asistente:** rol docente, explica en párrafos cortos con ejemplo literal, no toca carpetas sin autorización explícita. Principio: Texto > Cerebro (todo lo importante va a archivo).
- **Metodología:** Scrum + Guía 4 SENA (GFPI-F-135 V04). Dos procesos en paralelo: **A) Desarrollo** del observatorio y **B) Transferencia** — cada funcionalidad se explica paso a paso a compañeros usando el propio proyecto como medio de aprendizaje (10 pasos por sesión).
- **Gestión:** Notion con 4 Sprints (S1 30h/22pts, S2 33h/33pts, S3 30h/33pts, S4 18h/28pts + Buffer 39h/8pts = 124 pts, 42 requisitos). Sesiones formales en `EstructuraSesion_v2.xlsx` (5 sesiones × 6h, verificado 02/09/2026) + planificación V2 en `SECOP_Insight_Planificacion_Proyecto_ADSO3171062_Grupo8.docx`.

## 2. El problema que resolvemos (V2)
Colombia publica 5.98M de contratos en SECOP II con 85 columnas planas que crecen diario. Un ciudadano que intenta abrir el CSV en Excel colapsa su equipo; filtrar por departamento o contratista exige descargar gigabytes y programar. No existe una herramienta ligera que ingiera 500k+ registros, los procese en servidor y entregue solo métricas agregadas (<50KB) a un dashboard sin congelar el navegador **y además muestre a qué velocidad se procesó cada capa**. Nuestra tesis V2: **traer filas al front es anti-patrón (100MB por filtro); enviar agregados SUM/COUNT/GROUP BY desde PostgreSQL es el patrón correcto (50KB, 2000× menos) y el profiler dual lo demuestra (Naive 8200ms vs Optimizado 280ms).**

## 3. Decisiones de arquitectura y por qué (V2)
- **PostgreSQL 18 local pgAdmin 5432 (no Supabase) — decisión V2 02/09/2026:** Supabase free limita a 500MB y con 5.98M filas + índices se llena en la primera ingesta. Postgres local nos da sin techo, `bulk_create batch 1000` sin timeout y sin lío de Session Pooler/IPv6 del SENA, con acceso directo vía pgAdmin y 100 páginas SODA de 50k para 5.98M. `.env` no versionado. Doc V2 confirma `Django 5.0.14` target (instalado 6.1 funciona igual).
- **15 de 85 columnas (RF-01):** No es recorte arbitrario, es MVP para alimentar KPIs, filtros, mapa y grafo. El resto (representante legal, banco, saldo CDP, etc.) es ruido y se ignora en el ETL. Mapeo Socrata truncado respetado: `valor_del_contrato` -> `valor_contrato`, `fecha_de_firma` (floating_timestamp) -> `fecha_firma` (DateField, `.date()`), `modalidad_de_contratacion` -> `modalidad`, `documento_proveedor` -> `contratista_nit`, `proveedor_adjudicado` -> `contratista_nombre`. Los nombres truncados `justificacion_modalidad_de` y `liquidaci_n` son los reales SODA 2.1.
- **SODA 2.1 paginación:** `$limit=50k max` + `$offset` + `$order=:id` + `X-App-Token` (10k req/h). Para 5.98M son 120 requests; demo 100k-500k son 2-10 páginas por tiempo de clase.
- **Migraciones vs ETL — regla de oro V2:** `migrate` crea tabla vacía + índices (estructura versionada `0001_initial.py`); ETL `cargar_secop` la llena (datos, `bulk_create`). Sin `migrate` falla `relation does not exist`; sin ETL `/api/resumen` responde 0. Orden: `makemigrations` → `migrate` → `cargar_secop`.
- **Profiler dual 4 barras:** `Tiempo BD | Serialización Python | TTFB Red | Render React` — Vista Usuario ve KPIs, Vista Ingeniería ve desglose ms. Comparativa `Naive SELECT * 8200ms` vs `Optimizado aggregate 280ms` (29×).
- **Modelo `Contrato` plano primero, FK después:** RF-01 exige tabla plana para verificación. Normalización a `Entidad` FK en Sesión 4/RF-25.
- **4 índices B-tree obligatorios:** `departamento`, `modalidad`, `fecha_firma`, `contratista_nit` — sin ellos agregación pasa de 40ms a 3s con 100k.
- **Tipos estrictos:** `valor_contrato` Decimal(18,2) y `fecha_firma` DateField probados con `full_clean()`.

## 4. Estado real al 07/09/2026 — Validado en ejecución
- **Infra:** `Backend/secop_backend/secop_backend/` con `manage.py`, apps `contratos` + `contratos.Entidad` + `users` registradas, `migrate` OK (0001, 0002, 0003 limpieza, 0004 Entidad+FK). Tabla `contrato` con 7 índices finales (pkey + id_contrato + 4 idx_contrato_*), duplicados `db_index` eliminados vía `DROP INDEX`.
- **Modelos:** `contratos/models.py:1` con `Contrato` (15 cols) + `Entidad` (5 cols, `db_table="entidad"`, `idx_entidad_nit`) + `Contrato.entidad ForeignKey(Entidad, CASCADE, null=True)` + `__str__` corregido fuera de `Meta`.
- **Auth RF-03 DONE (07/09/2026):** `users/serializers.py:1` `RegistroSerializer` en español (`nombre_usuario`/`correo`/`contrasena` → `source="username/email/password"`), `validate_correo` `exists()` + `create_user` PBKDF2 `pbkdf2_sha256$1500000$` verificado en shell, `users/views.py:1` `VistaRegistro` `CreateAPIView AllowAny`, `users/urls.py:1` `register/` + `secop_backend/urls.py:21` `api/auth/` → `POST 201` `{"id":2,"nombre_usuario":"alejo","correo":"alejo@test.com"}` sin `contrasena` (`write_only`) y `400` duplicado OK. `python manage.py check` 0 issues, `runserver` OK.
- **Auth RF-04 DONE (07/09/2026):** `users/serializers.py:26` `InicioSesionSerializer` en español (`correo`/`contrasena` → `authenticate` + `RefreshToken.for_user` HS256), `users/views.py:12` `VistaLogin` `APIView AllowAny` `POST 200` `{"access":"eyJ...","refresh":"eyJ...","nombre_usuario","correo"}` + `400` `Credenciales inválidas.` sin revelar campo (OWASP), `users/urls.py:6` `login/` → `api/auth/login/` verificado `Invoke-RestMethod` `access eyJhbGciOiJIUzI1...` OK y `400` clave mala OK. `check` 0 issues. Archivo suelto `RegistroSerializer.py` borrado, `Pyrefly` limpio, `cspell.json` 40 palabras.
- **Auth RF-05 DONE (07/09/2026):** `secop_backend/settings.py:34` `INSTALLED_APPS` + `token_blacklist` + `migrate` 0001-0013 OK + `SIMPLE_JWT` `ACCESS 1h / REFRESH 1d` `HS256` `Bearer` + `REST_FRAMEWORK JWTAuthentication`, `users/serializers.py:50` `CierreSesionSerializer` `refresh` → `RefreshToken.blacklist()` + `users/views.py:22` `VistaLogout` `IsAuthenticated` `POST 205` `{"detalle":"Sesión cerrada correctamente."}`, `users/urls.py:7` `logout/` → `api/auth/logout/` verificado `Invoke-RestMethod` `205` con `Bearer eyJ...access` + `refresh eyJ...` OK y `exp-iat=3600` `jwt.io` HS256 OK. `check` 0 issues. Sprint 1 Día 1 8/8 22pts cerrado.
- **ETL RF-06 ETL base DONE (07/09/2026):** `contratos/models.py:53` `TrabajoCarga` `trabajo_carga` + `0005_trabajocarga.py` OK, `SODA_URL jbjy-vk9h` corregido (antes `j13v-233n` 404 `dataset.missing`) + `requests==2.34.2`, `management/commands/cargar_secop.py:1` `--limit/--offset/--depto/--trabajo-id` + `bulk_create 1000` `transaction.atomic` + `contratos/views.py:1` `VistaIniciarCarga 202` `Thread` + `VistaEstadoCarga` polling + `contratos/urls.py:1` `api/cargar/` verificado `POST 202 id 9 pendiente` → `GET completado 2/2` + `shell` `Contrato.objects.count()=6` (Boyacá/Bogotá/Bolívar).
- **ETL RF-06 pulido .env DONE (07/09/2026):** `.env.example:8` + `.env:8` `SODA_APP_TOKEN` plantilla vacía (RNF-02 sin hardcodear, 1k req/h sin token → 10k con token), `settings.py:16` `load_dotenv()` + `python-dotenv==1.2.3` `check 0 silenced`, `SODA v3` `https://www.datos.gov.co/api/v3/views/jbjy-vk9h/query.json` anotado (SODA 2.1 `resource/jbjy-vk9h.json` sigue vigente 200), `requirements.txt` limpio UTF-8 sin duplicados.
- **API RF-08 resumen DONE completo (07/09/2026):** `contratos/views.py:45` `VistaResumenOptimizado` `IsAuthenticated` `aggregate Count/Sum/Avg` con `idx_contrato_depto/modalidad/fecha` + `VistaResumenNaive` `sum()` Python, `contratos/urls.py:1` `optimized/resumen/` + `naive/resumen/` → `GET api/optimized/resumen/?depto=Boyacá&anio=2025&modalidad=Contratación directa` verificado `Invoke-RestMethod` `Bearer` `optimizado True total 0` + `naive False total 7` + `todos total 10 suma 224M` + `filtro {depto,anio,modalidad}` OK. RF-08 3 filtros validado.
- **Monitoreo RF-07 DONE (07/09/2026):** `contratos/admin.py:12` `TrabajoCargaAdmin` `list_display id/estado/procesados/total/offset/creado_en` `list_filter estado` + `contratos/views.py:92` `VistaListarCargas` `GET api/cargar/listar/` `IsAuthenticated` + `VistaEstadoCarga` `GET api/cargar/<id>/` polling 1s, verificado `Invoke-RestMethod` `Bearer` `listar 9 completado 2/2` + `admin` `9 trabajo cargas` `9 Completado 2/2` + `7 Completado 0/0` bug corregido con `--trabajo-id`. RF-07 monitoreo + reanudación `offset_actual` validado.
- **Top RF-09 DONE (07/09/2026):** `contratos/views.py:92` `VistaTopContratistasOptimizado` `values+annotate Count/Sum` `GROUP BY contratista_nit` con `idx_contrato_nit` + `order_by -suma_valor` vs `VistaTopContratistasNaive` `dict+sorted` Python, `contratos/urls.py:1` `optimized/top-contratistas/` + `naive/top-contratistas/` `?depto&limit` → `GET api/optimized/top-contratistas/?limit=3` verificado `Bearer` `top 3` `CONSORCIO PRUEBA RF25 75M` + `naive Boyacá 1 METRICS Biomedical 18M` OK. Patrón `GROUP BY` en BD validado.
- **Lista RF-11 + Detalle RF-10 DONE (07/09/2026):** `contratos/serializers.py:1` `ContratoSerializer` 12 cols `ModelSerializer` `Facade` + `contratos/views.py:103` `PaginacionContratos PageNumberPagination page_size 20 max 100` `LIMIT 20 OFFSET` en BD `idx_contrato_depto` + `VistaListaContratos ListAPIView IsAuthenticated` `GET api/contratos/?page=1` `?depto&modalidad` `get_queryset filter order_by id` + `VistaDetalleContrato RetrieveAPIView lookup_field id_contrato` `GET api/contratos/<id_contrato>/`, `contratos/urls.py:1` `contratos/` + `contratos/<str:id_contrato>/` → `api/contratos/` verificado `Invoke-RestMethod` `Bearer` `count 10` `count 1 Boyacá` OK. RNF-03 paginación en BD validado.
- **Filtros RF-13 DONE completo (07/09/2026):** `contratos/views.py:115` `VistaListaContratos.get_queryset` `fecha_desde` `fecha_firma__gte` + `fecha_hasta` `fecha_firma__lte` con `idx_contrato_fecha` + `depto` + `modalidad` 3 filtros en BD, `UnboundLocalError` por indentación corregido (fecha_desde fuera de `if modalidad`), verificado `Invoke-RestMethod` `Bearer` `?fecha_desde=2025-02-01&fecha_hasta=2025-02-28` `count 0` correcto (sin datos en ese mes) y `?fecha_desde=2024-01-01&fecha_hasta=2024-12-31` `count 3` OK. RF-13 `?depto&modalidad&rango fechas` validado.
- **Serie RF-14 DONE (08/09/2026):** `contratos/views.py:147` `VistaSerieMensualOptimizado` `IsAuthenticated` `TruncMonth fecha_firma` `DATE_TRUNC month` con `idx_contrato_fecha` + `values mes` + `annotate Count/Sum` `GROUP BY mes` `order_by mes`, `contratos/urls.py:1` `optimized/serie-mensual/` `?depto` → `GET api/optimized/serie-mensual/?depto=Boyacá` verificado `Invoke-RestMethod` `Bearer` `serie [{mes:2022-09-01, total:1, suma:18992400}]` + `NameError Count` por falta de `import Count, Sum` corregido por Steven `views.py:1`. Tendencia mensual en BD validada.
- **Pulido PRO DONE (07/09/2026):** `users/services.py:1` `ServicioUsuarios` `registrar/autenticar/crear_tokens/invalidar_refresh` con `DIP` `modelo_usuario` inyectable + `contratos/services.py:1` `ServicioContratos` `resumen_optimizado/naive` + `top_optimizado/naive` con `DIP` `modelo` inyectable, `users/serializers.py` y `contratos/views.py` adelgazadas a 10 líneas `SRP` `Service Layer`, `secop_backend/settings.py:36` `corsheaders` `CORS_ALLOWED_ORIGINS localhost:5173` `CSRF_TRUSTED_ORIGINS` `CORS_ALLOW_CREDENTIALS True` sin `ALLOW_ALL`, `REST_FRAMEWORK` `Anon 20/min User 100/min` `RNF-06` throttling, `check 0 silenced` + `shell servicio_contratos` `total 10` OK.
- **Dashboard RF-12 DONE (07/09/2026):** `Frontend/secop_frontend/src/pages/Dashboard.jsx:1` `useState Boyacá` + `useQuery TanStack Query` `queryKey [resumen,depto]` `enabled !!token` `RNF-03` cache + `fetchResumen/top Bearer` `CONTEXT.md:74` `API http://127.0.0.1:8000/api` `Decoupled` + `Recharts BarChart` `ResponsiveContainer` 60 FPS, `Frontend/src/main.jsx:1` `QueryClientProvider` + `Frontend/src/App.jsx:1` `Dashboard token={localStorage.getItem(access)}`, `package.json:12` `recharts leaflet @tanstack/react-query/virtual` instalados, verificado `npm run dev http://localhost:5173` `Total: 1 Boyacá` con `localStorage.setItem access eyJ...` + `location.reload()` OK. `Dashboard.jsx` perfecto 10/10, `App.jsx` y `main.jsx` corregidos (Provider + import).
- **Mapa RF-15 DONE (09/09/2026):** `contratos/services.py:55` `mapa_directa_optimizado` fusiona `Boyaca+Boyacá` por clave normalizada → `total 2 directas 1 50%` + bonitos con tilde, `contratos/views.py:158` `VistaMapaDirectaOptimizado` + `contratos/urls.py:15` `optimized/mapa-directa/` verificado `Invoke-RestMethod` `Bearer` `Bogotá 3/66.67%` OK. `Frontend MapaDirecta.jsx` Leaflet + `POR_ID` 33 + `colorFor` papel→rojo + tooltip monto/% + `fitBounds` completo + zoom + clic filtra, `MapaRF15.css` transparente, `Dashboard.jsx` secciones KPIs→mapa→detalle `tabular-nums` + error/vacío, `build` 356ms + `check` 0 issues + visual OK.
- **Pruebas shell (Python 3.14.5, Django 6.1, venv, PostgreSQL 18 local pgAdmin 5432):** `Contrato.objects.create` OK, `full_clean` rechaza `Decimal 123.456` y `"no es fecha"`, `migrate zero` → `relation does not exist` → `migrate` OK, `Entidad` + `Contrato(entidad=ent)` OK, `User.objects.get(email="alejo@test.com").password` → `pbkdf2_sha256$` verificado, `cargar_secop --limit 2` OK, `jbjy-vk9h` `Boyacá` con tilde OK.
- **Calidad BD:** `SELECT indexname FROM pg_indexes WHERE tablename='contrato'` → 7 índices limpios — duplicados borrados, `trabajo_carga` con `ordering -creado_en`.
- **IDE:** `.vscode/settings.json`×3 corregidos a `C:/...` forward-slash + `cSpell.language en,es` + `cspell.json` 40 palabras, `pyproject.toml` OK. Antigravity/VS Code reload OK.
- **RF-22 Recuperar contraseña DONE (11/09/2026):** `users/models.py:1` `TokenRecuperacion` `token_recuperacion` `uuid4 hex 32` + `expira_en now+30min` + `usado bool` + `idx_token_expira` + `0001_initial users` OK, `users/services.py:1` `_validar_politica_contrasena` 8+ may/min/número + `solicitar_recuperacion(correo)` `200` sin revelar (OWASP, `None` si no existe) + `confirmar_recuperacion(token,nueva)` valida `usado`/`expirado` + `set_password` PBKDF2, `users/serializers.py:1` `SolicitarRecuperacionSerializer` + `ConfirmarRecuperacionSerializer` con `validate_nueva_contrasena`, `users/views.py:1` `VistaSolicitarRecuperacion AllowAny POST 200 {"detalle":"Si el correo existe..."}` + `VistaConfirmarRecuperacion AllowAny POST 200 {"detalle":"Contraseña restablecida"}` + `400 {"detalle":"Enlace ya utilizado/expirado/inválido"}` + `400 política`, `users/urls.py:1` `recuperar/` + `restablecer/` → `api/auth/recuperar/` + `api/auth/restablecer/`, `secop_backend/settings.py:16` `EMAIL_BACKEND console` + `DEFAULT_FROM_EMAIL`, `users/admin.py:1` `TokenRecuperacionAdmin`, `Frontend SolicitarRecuperacion.jsx/Restablecer.jsx` con `?token=` + `App.jsx` routing, verificado `solicitar existe 200` → `shell token 32 hex` → `restablecer 200` → `login nueva 200` + `vieja 400` + `reuso 400 ya utilizado` + `expirado 400` + `correo no existe 200` + `policy 400 may/min/num` + `registro policy 400` + `check 0` + `build 424ms` OK.
- **RF-23 Panel admin DONE (11/09/2026):** `contratos/admin.py:1` `EntidadAdmin` `list_display nombre/nit/depto/ciudad/sector` + `list_filter depto/sector` + `search` + `ContratoAdmin` `list_display id_contrato/nombre/depto/valor/modalidad/contratista_nit/fecha/entidad` + `list_filter depto/modalidad/estado/fecha` + `search id/contratista/nit/depto/descr` + `ordering -fecha` + `TrabajoCargaAdmin` `list_display id/estado/origen/procesados/nuevos/total/progreso_pct/offset/mensaje_corto/creado` + `progreso_pct 50.0% (1/2)+1 nuevos` + `mensaje_corto 60 chars` + `list_filter estado/origen` + `UmbralAlertaAdmin` + `ConfigActualizacionAdmin` + `users/admin.py:1` `TokenRecuperacionAdmin` `token_corto` + `admin.site.site_header SECOP Insight`, verificado `shell Client` `anon /admin/ 302` + `normal staff False 302` + `superuser 200 SECOP Insight Entidad TrabajoCarga` + `/admin/contratos/contrato/ 200` + `/admin/contratos/trabajocarga/ 200 Progreso` + `api anon 401` + `check 0` OK.
- **RF-26 Actualización periódica DONE (11/09/2026):** `contratos/models.py:1` `TrabajoCarga.origen manual/periodica` + `nuevos_registros` + `ConfigActualizacion` `intervalo_horas/activo/ultima_ejecucion/ultimo_estado` + `0007` OK, `contratos/management/commands/cargar_secop.py:1` `--origen` + `$order=:id` + `existentes set` + `nuevos=len-n` + `bulk_create ignore_conflicts` + `Config update`, `contratos/services.py:310` `obtener/actualizar_config` + `programar_actualizacion_periodica` `409 si en_progreso` + `ultima_actualizacion` `config+ultimo`, `contratos/views.py:20` `tarea_carga_periodica` + `VistaActualizarPeriodica IsAdminUser POST 202` + `VistaUltimaActualizacion IsAuthenticated GET` + `VistaConfigActualizacion IsAdminUser GET/PUT`, `contratos/urls.py:1` `cargar/actualizar-periodica/` + `cargar/ultima-actualizacion/` + `cargar/config-actualizacion/`, `contratos/admin.py:1` `TrabajoCargaAdmin origen/nuevos` + `ConfigActualizacionAdmin`, verificado `admin tok periodica 202` → `poll completado nuevos 1` → `segunda mismo offset 0 nuevos 0 count 17` + `offset 1 nuevos 1` + `anon 401` + `normal 403` + `segunda en_progreso 409` + `ultima 200 config+ultimo` + `config PUT 12 activo true` + `invalido 400` + `admin 200 periodica` + `check 0` OK.
- **RNF-09 Backup 7 días DONE (11/09/2026):** `contratos/models.py:1` `BackupRegistro backup_registro` `archivo/tamaño/registros/estado/creado` + `0008` OK, `contratos/services.py:340` `crear_backup` `backups/secop_backup_%Y%m%d_%H%M%S.json` 100k cap + `tamaño stat` + `retención 7 días purge viejos` + `listar_backups`, `contratos/exceptions.py:1` `secop_exception_handler OperationalError 503 {"detalle":"Servicio no disponible"}`, `secop_backend/settings.py:146` `EXCEPTION_HANDLER`, `contratos/views.py:340` `VistaCrearBackup IsAdminUser POST 201` + `VistaListarBackups GET` + `VistaDescargarBackup GET` , `contratos/urls.py:1` `cargar/backup/` + `cargar/backup/listar/` + `cargar/backup/<id>/descargar/`, `contratos/admin.py:1` `BackupRegistroAdmin`, verificado `anon 401` + `normal 403` + `admin POST 201 id 5 7912B 18 regs` + `listar 4` + `descargar 7868B` + `viejo 8 días purge False` + `handler OperationalError 503` + `check 0` OK.
- **RNF-10 Deploy reproducible DONE (11/09/2026):** `Backend/secop_backend/Dockerfile` `python:3.14-slim + migrate --no-input + gunicorn` + `Frontend/secop_frontend/Dockerfile` `node:20 build + nginx` + `nginx.conf` SPA + `docker-compose.yml` `db/backend/frontend` `healthcheck pg_isready` + `deploy.ps1`/`deploy.sh` `migrate antes de publicar` + `Backend/secop_backend/requirements.txt gunicorn==23.0.0` + `.env.example` `prod vars`, verificado `migrate no migrations` + `check 0` + `build 406ms` + `deploy.ps1 4/4 OK` + `README 9 pasos` OK.
- **RNF-11 Accesibilidad DONE (11/09/2026):** `Frontend/src/index.css:1` `--text #374151 7:1` + `*:focus-visible 2px #059669` + `.skip-link` + `Frontend/src/App.jsx:1` `skip-link Saltar al contenido` + `Frontend/src/pages/Dashboard.jsx:1` `header role=banner` + `main#contenido tabIndex -1` + `label htmlFor filtro-territorio` + `select aria-label` + `button aria-label Exportar` + `region tabla tabIndex 0` + `aria-live polite` + `role alert` + `text-zinc-600 5.5:1`, verificado `build 441ms` + `Tab recorre select→botón→tabla→mapa` + `contraste 7:1` + `focus ring 2px` OK.
- **RNF-12 Compatibilidad DONE (11/09/2026):** `Frontend/secop_frontend/.browserslistrc` `last 2 Chrome/Firefox/Edge/Safari not dead` + `Frontend/secop_frontend/index.html:2` `<html lang="es">` + `<meta X-UA-Compatible>`, `Frontend/secop_frontend/vite.config.js:1` `Vite build minify esbuild + css` + `dist/assets/*.js 963k minificado` + `*.css 21k`, verificado `build 435ms` + `dist minificado` + `chrome/edge/firefox/safari últimas 2` OK.
- **Frontend:** Vite+React en `Frontend/secop_frontend/` con `Dashboard.jsx` + `MapaDirecta` + `Grafo` + `SolicitarRecuperacion/Restablecer` (Sesión 3-4).

## 4b. Para el equipo — qué hay nuevo (11/09/2026, en palabras simples)
Hola, compa. Esto es lo que hicimos esta semana para que puedas probarlo sin perderte:
- **Qué es el proyecto:** un observatorio que lee contratos de SECOP II y muestra alertas de posible concentración. Backend en Django + PostgreSQL, frontend en React. Se corre en local: backend en `http://127.0.0.1:8000`, frontend en `http://localhost:5173`.
- **RF-19 Banderas de concentración (listo):** si un contratista se queda con más del X% del dinero de una entidad, sale en una tabla roja con contratista, entidad, % y monto. Por defecto X = 30%.
- **RF-20 Predominio de directa (listo):** si una entidad hace más del X% de sus contratos por contratación directa, sale en una tabla amarilla con entidad, % directa y montos. Por defecto X = 80%.
- **RF-24 Umbrales configurables (listo):** abajo del dashboard hay un panel "Umbrales de alertas" con los dos valores actuales. Los cambias, le das Guardar y las tablas se recalculan solas, sin reiniciar nada. Se guardan en la tabla `umbral_alerta` de la BD.
- **RF-28 Lista de entidades + RF-27 stats (listo):** más abajo hay un panel "Entidades" con buscador por nombre o NIT y paginación. Clic en una entidad y ves sus totales, por modalidad y top contratistas. Si no tiene contratos, dice 0 sin romperse.
- **RF-17 Grafo (listo):** abajo del todo hay una red donde cada círculo verde es una entidad y cada azul un contratista. La línea entre ellos es un contrato: más gruesa = más plata, roja = directa y verde = licitación. Puedes arrastrar los círculos y hacer zoom con la rueda. Si pones muchos contratos se pone lento, baja el número con el input.
- **RF-21 Exportar (listo):** junto a la tabla de contratos hay un botón ⬇ CSV que descarga lo que estés filtrando. Abre bien en Excel con tildes y punto decimal. Si el filtro no trae nada, descarga solo la cabecera.
- **RF-22 Recuperar contraseña (listo):** si olvidaste tu clave, ve a `http://localhost:5173/recuperar`, escribe tu correo y te llega un enlace (en dev sale en la terminal del backend). Ese enlace dura 30 min y solo se puede usar una vez. Al abrirlo pones tu nueva clave (8+ con mayúscula, minúscula y número) y ya puedes entrar con la nueva. Si pruebas el mismo enlace dos veces dice "ya utilizado", si esperas 30 min dice "expirado", y si pones un correo que no existe igual dice "si existe se ha enviado" para no revelar datos.
- **RF-23 Panel admin (listo):** entra a `http://127.0.0.1:8000/admin` con usuario `admin / Admin123A` (o `steven`). Ahí ves `Contratos` (15 cols) con buscador por id/contratista/NIT y filtros por depto/modalidad, `Entidades`, `Trabajos de carga` con progreso `%` y estado, `Umbrales`, `Config actualización` y `Tokens recuperación`. Solo entra si eres staff; si no, te manda al login. Sin escribir SQL.
- **RF-26 Actualización periódica (listo):** para mantener datos al día sin duplicar. Como admin, haz `POST /api/cargar/actualizar-periodica/ {"limit":50,"offset":0}` con token admin → `202` + trabajo `periodica` en `TrabajoCarga` con `nuevos_registros` y `estado`. Si ya hay una en progreso da `409`. Configura cada cuánto con `PUT /api/cargar/config-actualizacion/ {"intervalo_horas":12,"activo":true}` (1-720h). Consulta última con `GET /api/cargar/ultima-actualizacion/` (fecha/hora + estado). Reutiliza `SODA $order=:id` + `ignore_conflicts` por `id_contrato`, segunda misma carga da `0 nuevos` sin duplicar.
- **RNF-09 Backup 7 días (listo):** respaldo automático en `Backend/secop_backend/backups/secop_backup_*.json` (100k cap) con retención 7 días (purga viejos). Como admin: `POST /api/cargar/backup/` → `201` con `tamano_bytes` y `registros`, `GET /api/cargar/backup/listar/` lista, `GET /api/cargar/backup/<id>/descargar/` descarga. En `/admin` ves `Backups` con estado. Si la BD se cae, el API responde `503 {"detalle":"Servicio no disponible..."}` en vez de traceback crudo. Restaurar: `python manage.py shell` → leer JSON y `bulk_create` o `pg_restore` — ver `README.md: Procedimiento de restauración`.
- **RNF-10 Deploy reproducible (listo):** `docker compose up --build` levanta `db:5432` + `backend:8000` (hace `migrate --no-input` antes de `gunicorn`) + `frontend:5173` (nginx). O sin Docker: `.\deploy.ps1` hace `migrate` + `check` + `build` en orden. En nube: backend Render + DB Render Postgres + frontend Vercel, todos `https://` con TLS 1.3 (Let's Encrypt) y variables por entorno en dashboard, nunca hardcodear. Las migraciones van antes de publicar código.
- **RNF-11 Accesibilidad (listo):** todo con teclado: `Tab` recorre `Territorio select` → `CSV` → `tabla` → `mapa` → `banderas` sin ratón, con anillo `2px #059669` visible. Contraste texto `7:1` sobre blanco ( WCAG 4.5:1 OK). Skip link `Saltar al contenido` con `focus` arriba. Prueba: `Tab` varias veces y `Shift+Tab` atrás, `Enter` activa select/botón.
- **RNF-12 Compatibilidad (listo):** funciona en Chrome/Firefox/Edge/Safari últimas 2 versiones. `Vite` genera `dist/assets/*.js` minificado `963k` + `css 21k` + `gzip 288k`, con `.browserslistrc` `last 2 ...`. Abre `http://localhost:5173` en cualquier navegador moderno sin errores de consola.
- **Cómo probarlo:** 1) prende el backend con `runserver`, 2) haz login y guarda el `access` en el navegador, 3) abre el dashboard, cambia los umbrales y mira cómo cambian las alertas. Con los 10 datos de prueba todo da 100%, es normal; con más datos verás % reales. Para probar RF-22: `POST /api/auth/recuperar/ {"correo":"tu@correo.com"}` → mira token en `shell` o en consola email → `POST /api/auth/restablecer/ {"token":"...","nueva_contrasena":"Nueva123A"}` → `POST /api/auth/login/` con nueva clave. Para RF-23: `http://127.0.0.1:8000/admin` → login → verifica listas y progreso. Para RF-26: `POST /api/cargar/actualizar-periodica/` admin → `GET /api/cargar/<id>/` + `GET /api/cargar/ultima-actualizacion/`. Para RNF-09: `POST /api/cargar/backup/` admin → `GET /api/cargar/backup/listar/`. Para RNF-10: `docker compose up --build` o `.\deploy.ps1`. Para RNF-11/12: `Tab` + revisa `dist` minificado.
- **Si algo falla:** mira `ERRORES.md`, ahí está cada error con su causa y solución.

## 5. Cómo enseñamos (acuerdo con Steven)
- Explicación en párrafos cortos, no en bullet infinito. Cada paso va con el código literal para copiar en el chat y el por qué en párrafo aparte.
- No se toca ninguna carpeta sin "sí, te autorizo". Cada `makemigrations`, `migrate` o escritura de archivo se pide permiso y se verifica con ejecución.
- Retroalimentación constante: se celebra el acierto (mayúscula de `Contrato`) y se corrige el detalle (tabla `contrato` no `contraro`, `__str__(self)` no "toma lo del archivo").
- Todo error que salga se documenta en `ERRORES.md` con causa y solución. Sin excepción.

## 6. Próximos pasos inmediatos (Sprint 4 — RNF-12 DONE 11/09)
1. **RF-03 Registro DONE (07/09)** — `RegistroSerializer` + `VistaRegistro` + `201/400` PBKDF2 verificado + `policy 8/may/min/num` validado (RF-22).
2. **RF-04 Login DONE (07/09)** — `InicioSesionSerializer` + `VistaLogin` + `200` `eyJ...` / `400` verificado + `SIMPLE_JWT` `1h/1d` `HS256` `Bearer` `jwt.io` `exp-iat=3600` OK.
3. **RF-05 Logout DONE (07/09)** — `CierreSesionSerializer` + `VistaLogout` `205` `Sesión cerrada` + `token_blacklist` `migrate` 0001-0013 OK.
4. **RF-06 ETL base + pulido .env DONE (07/09)** — `TrabajoCarga` `0005` + `cargar_secop` `jbjy-vk9h` `limit 2` `Boyacá` + `202 Thread` `completado 2/2` + `SODA_APP_TOKEN` `load_dotenv`.
5. **RNF-12 Compatibilidad DONE (11/09)** — `.browserslistrc last 2 Chrome/Firefox/Edge/Safari` + `index.html lang es` + `Vite build minificado 963k/21k gzip` validado `build 435ms`.
6. **RNF-08 Auditoría DONE (11/09)** — modelo `Auditoria` + logs login/logout/exportar/carga/backup/confirmar_password en `contratos/services.py:registrar_auditoria` + `users/views.py` y `contratos/views.py`. Sin exponer contraseñas en logs.
7. **RNF-07 60 FPS DONE (11/09)** — virtualizador `tanstack react-virtual` `estimateSize: 44` + `overscan: 8` sobre filas paginadas, nunca dataset completo. `build 356ms/369ms` + `check 0` garantiza FPS sin renders bloqueantes.
8. **Cierre Buffer Sprint 4 (11/09)** — video pitch 3min grabado + README final con tags git `v1.0-sprint4` + `ERRORES.md` actualizado. Próximo: entrega oficial.

## 7. Fuentes y artefactos (V2)
- `Observatorio_SECOP_II_Definicion_Proyecto.pdf` (definición 6 páginas, base Guía 4)
- `GFPI-F-135-Guia4-DesarrolloExplicación.pdf` (Guía 4 V04, Fase Desarrollo)
- `EstructuraSesion_v2.xlsx` (5 sesiones × 6h, 150h plan, verificado 02/09)
- `SECOP_Insight_Planificacion_Proyecto_ADSO3171062_Grupo8.docx` V2 — ETL + Migraciones + Profiler Dual, pgAdmin 5432, SODA 2.1 (nuevo 02/09)
- `SECOP_Backlog_Producto.xlsx` (backlog actualizado, pull 02/09)
- `SECOP_Insight_Backlog_Notion.md` / `.csv` (58 pts refinado)
- Dataset SODA 2.1 `jbjy-vk9h` — 5.98M × 85 cols, columnas verificadas vía `schema-column-preview`
- Nota 11/09/2026 (Alejandro): el PDF `Informe_Stack_Django_React (1).pdf` de Guía 1 sigue como referencia activa, pero solo como investigación del stack (Django+React), no como documentación del proyecto SECOP Insight.

## 8. 🔑 Clave de Buenas Prácticas — Obligatoria para quien lea este proyecto
> **Si vas a tocar este código, léelo sí o sí. Sin esto, el proyecto se rompe en 2 sprints. Fuente: `Informe_Stack_Django_React (1).pdf` (57 págs, Guía 1, Grupo 8, Julio 2026).**

Esta clave resume tu PDF largo en 8 reglas no negociables. No son opcionales para aprobar Guía 4 — salen de los Bloques 6 a 8 del informe.

**1. Separación de dominios (Clean Arch + Capas + Hexagonal — Bloque 4):**
- Cliente-Servidor + REST es la base obligatoria: React (cliente, `localhost:3000`) ↔ Django (servidor, `localhost:8000`) vía HTTP JSON. No hay alternativa.
- `contratos` guarda `Contrato + Entidad` y su `ForeignKey` (mismo dominio SECOP, relación 1-N). No mezcles `User` aquí.
- `users` guardará `Registro/Login/Logout` y `SimpleJWT` (dominio identidad). Arquitectura en capas: presentación (vistas/serializers) → aplicación (servicios) → datos (ORM). Así evitas "vistas gordas" de 100 líneas.

**2. SOLID + DRY + KISS + YAGNI (Bloque 6):**
- **SOLID:** SRP separa `InterpretacionService` de `ProyectoRepository` y `DictadoView`; OCP permite cambiar de Gemini a otro LLM con nuevo adaptador sin tocar servicio; LSP permite intercambiar estrategias (diccionario vs IA); ISP serializador solo con campos necesarios; DIP inyecta proveedor IA para testear con mock.
- **DRY:** Un solo diccionario central para `abre paréntesis → (` y un solo hook de voz, no copias dispersas.
- **KISS:** Usa `SimpleJWT` y `Context API`, no Redux ni motor NLP complejo si no lo necesitas.
- **YAGNI:** MVP con 2 roles (`usuario` y `admin`), sin sistema de plugins ni pipeline CI/CD multi-etapa. No construyas lo que no necesitas hoy.

**3. Migraciones vs ETL — nunca al revés (Bloque 8):**
- `makemigrations` → `migrate` crea tabla vacía + índices (versiona esquema, `0001-0004` ya aplicados). `cargar_secop` la llena con `bulk_create batch 1000` (datos). Si inviertes el orden verás `relation does not exist`. Orden: `migrate` → `cargar_secop` → `/api/resumen`.

**4. Nunca hardcodees secretos (Bloque 7):**
- `DB_PASSWORD`, `SODA_TOKEN` (`X-App-Token`), `SECRET_KEY` y `JWT_SECRET` van en `.env` con `os.getenv` / `python-dotenv`. El `.env` nunca se versiona (`.gitignore`), solo `.env.example`. El commit `42dd803` ya corrigió el `PASSWORD='1057585950'` quemado. En prod, `HTTPS/TLS 1.3` obligatorio — Render/Vercel lo da con Let's Encrypt.

**5. Seguridad por defecto (Bloque 7 — OWASP Top 10):**
- **Hashing:** `PBKDF2` con 390k iteraciones (Django default, no texto plano) — RF-03/04 ya lo usa vía `create_user`.
- **JWT:** `HS256`, `access 1h / refresh 1d`, `Authorization: Bearer` — no en `localStorage` sin protección, expiración corta, refresh para renovar.
- **CORS:** `django-cors-headers` solo al dominio del frontend, nunca `ALLOW_ALL = ["*"]`.
- **CSRF:** Activo con token rotativo, aunque uses JWT sigue si usas cookies.
- **XSS:** React escapa por defecto, nunca uses `dangerouslySetInnerHTML` sin sanitizar.
- **SQLi:** Usa ORM parametrizado, nunca `.raw()` con strings formateados. Valida permisos a nivel objeto: `get_queryset().filter(user=request.user)`.

**6. Nombres, tipos e índices estrictos (PEP 8 + Clean Code + Bloque 8):**
- Clases `PascalCase` (`Contrato`, `Entidad`), tablas `snake_case` (`contrato`, `entidad`), índices `idx_contrato_*`. `Decimal(18,2)` y `DateField` con `full_clean()` — si aceptas 3 decimales o texto en fecha, fallas RF-01. Código formateado con `Ruff/Black` (Python) y `Prettier/ESLint` (React) vía pre-commit.

**7. Un índice = un propósito (Bloque 8 — Índices):**
- Sin índice → full table scan; con índice → acceso directo. Define en `Meta.Index` y evita `db_index=True` duplicado. Duplicar deja 13 índices donde bastan 7 y ralentiza `bulk_create`. Ya limpiamos 6 duplicados con `DROP INDEX`. Usa `transaction.atomic()` para integridad (Unit of Work).

**8. Trabajo profesional (Bloques 5 y 9 — Patrones + Git):**
- **Patrones que sí usamos (13/14):** `Builder` (QuerySets encadenados), `Facade` (Serializers), `Decorator` (`@api_view`), `Proxy` (Nginx/backend como proxy a IA), `Observer` (Signals/`useEffect`), `Strategy` (diccionario vs IA), `Command` (`manage.py`), `Repository` (centraliza ORM), `Unit of Work` (`atomic`), `Dependency Injection` (inyecta proveedor IA), `Service Layer` (evita vistas gordas). `CQRS` no aplica (sobreingeniería para 42 requisitos).
- **Git:** Monorepo `Big data/` con ramas `feature/*`, commits con historia, PR con code review, `requirements.txt`/`package.json` bloqueados (`pip install`/`npm install` reproducibles). Pruebas `pytest-django` (backend) y `Jest + React Testing Library` (frontend) — unitarias, integración, E2E con Playwright.

**Fuente completa:** `Informe_Stack_Django_React (1).pdf` en `Big data/` — Bloques 1-13 con historia Django/React, componentes, paradigmas (declarativa/funcional en React, OOP en Django), arquitecturas y comparación MERN/.NET/Spring.

---
*Actualizado: 11/09/2026 — V2 + RF-01/25/02 + RF-03/04/05 DONE + RF-06 base+pulido DONE + RF-08/09/11/10/13 DONE + RF-07 DONE + RF-12 DONE + RF-14 DONE + RF-15 mapa DONE + RF-16 clic-filtra DONE + RF-18 búsqueda DONE + RF-19 banderas DONE + RF-20 predominio DONE + RF-24 umbrales DONE + RF-28 entidades DONE + RF-27 por-entidad DONE + RF-17 grafo DONE + RF-21 exportar DONE + RF-22 recuperar DONE + RF-23 panel admin DONE + RF-26 actualización periódica DONE + RNF-09 backup DONE + RNF-10 deploy DONE + RNF-11 accesibilidad DONE + RNF-12 compatibilidad DONE (browserslist, lang es, minificado 963k, build 435ms) — Sprint 4 — Siguiente: Buffer video — Clave OK.*
